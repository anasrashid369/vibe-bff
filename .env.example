# Local dev / LocalStack only — real deployments use Secrets Manager,
# never plaintext env vars for provider keys (see src/services/secrets.ts).
AWS_ENDPOINT_URL=http://localhost:4566
AWS_REGION=us-east-1
PROVIDER_SECRETS_ID=vibe/provider-keys
# TMDB_READ_ACCESS_TOKEN=
# GEMINI_API_KEY=
# ANTHROPIC_API_KEY=
# GROQ_API_KEY=
